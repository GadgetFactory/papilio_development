[env:papilio_arcade]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
build_type = debug
board_build.arduino.memory_type = qio_qspi
board_build.flash_mode = qio
board_build.psram_type = qio
board_upload.flash_size = 4MB
board_upload.maximum_size = 4194304
board_build.partitions = default.csv

build_flags = -I include
              -DARDUINO_USB_CDC_ON_BOOT=1
              -DBOARD_HAS_PSRAM

board_build.filesystem = littlefs
monitor_speed = 115200
monitor_filters = esp32_exception_decoder
lib_deps = fastled/FastLED@^3.9.4
lib_extra_dirs = libs
; Clean up any leftover example files before build
extra_scripts = pre:src/utilities/cleanup_examples.py
; Exclude dynamically copied example file from base template build
build_src_filter = +<*> -<__active_example.ino>

; Note: Dual USB CDC requires ESP-IDF component approach which has compatibility
; issues with Arduino-ESP32's pre-built TinyUSB. For now, MCP uses the same
; serial port with protocol framing. See docs for protocol details.
; The environment below is kept for future development when Arduino-ESP32
; adds native support for multiple CDC interfaces.

; [env:papilio_arcade_dual_cdc]
; platform = espressif32
; framework = arduino, espidf
; ... (disabled pending Arduino-ESP32 dual CDC support)

; Text mode test environment
[env:text_mode_test]
extends = env:papilio_arcade
; Use pre-build script to copy example into src/__active_example.ino
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Space Invaders HDMI demo
[env:spaceinvaders_hdmi]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Space Invaders HQVGA demo (160x120 pixel graphics)
[env:spaceinvaders_hqvga]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Bricks HQVGA demo (160x120 pixel graphics)
[env:bricks_hqvga]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; VGA LCD Demo (LiquidCrystal-compatible API)
[env:vga_lcd_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Adafruit GFX Demo (graphics primitives)
[env:gfx_demo]
extends = env:papilio_arcade
lib_deps = 
    ${env:papilio_arcade.lib_deps}
    adafruit/Adafruit GFX Library@^1.11.0
    adafruit/Adafruit BusIO@^1.14.0
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; LVGL Demo (GUI widgets)
[env:lvgl_demo]
extends = env:papilio_arcade
lib_deps = 
    ${env:papilio_arcade.lib_deps}
    lvgl/lvgl@^8.3.0
build_flags = 
    ${env:papilio_arcade.build_flags}
    -DLV_CONF_INCLUDE_SIMPLE
    -I libs/papilio_hdmi/examples/lvgl_demo
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; U8g2 Demo (pixel-perfect bitmap fonts)
[env:u8g2_demo]
extends = env:papilio_arcade
lib_deps = 
    ${env:papilio_arcade.lib_deps}
    olikraus/U8g2@^2.35.0
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; TFT_eSPI-style API Demo
[env:tft_espi_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Image Decoder Demo (JPEG, PNG, GIF)
[env:imagedec_demo]
extends = env:papilio_arcade
lib_deps = 
    ${env:papilio_arcade.lib_deps}
    bitbank2/JPEGDEC@^1.8.4
    bitbank2/PNGdec@^1.1.6
    bitbank2/AnimatedGIF@^2.2.0
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Butterfly Fullscreen Image Display
[env:butterfly_fullscreen]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Video Modes Demo (demonstrates all 3 video modes)
[env:video_modes_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Test Pattern Demo (color bars, grid, grayscale)
[env:test_pattern_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; MCP Debug Firmware - for AI-assisted FPGA debugging
; Source comes from libs/papilio_mcp_server/examples/mcp_debug_simple
[env:mcp_debug_firmware]
extends = env:papilio_arcade
build_flags = ${env:papilio_arcade.build_flags}
              -DPAPILIO_MCP_ENABLED
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; MCP Breakpoint Demo - demonstrates advanced debugging features
[env:mcp_breakpoint_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; SID 6581 Audio Demo - Commodore 64 sound chip
[env:sid_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; YM2149 Audio Demo - AY-3-8910 PSG sound chip
[env:ym2149_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino> +<__active_example.ino>

; YM Player Demo - plays .ymd music files from LittleFS
[env:ym_player_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>
board_build.filesystem = littlefs

; POKEY Audio Demo - Atari sound chip
[env:pokey_demo]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; SID Player Demo - plays .sid files using 6502 emulation
[env:sid_player]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; SID Player with Filesystem - loads .sid files from LittleFS
[env:sid_player_fs]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>

; Audio MOD Player with DMA - hardware-accelerated audio streaming
[env:audio_mod_player_dma]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>
lib_deps =
    ${env:papilio_arcade.lib_deps}
    earlephilhower/ESP8266Audio @ ^1.9.7
board_build.filesystem = littlefs

; Audio Test Tone with DMA - sine wave test tone generator with MCP debug
[env:audio_test_tone_dma]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>
build_flags = 
    ${env:papilio_arcade.build_flags}
    -D PAPILIO_MCP_ENABLED

; Video DMA Test - tests DMA by writing patterns to framebuffer
[env:video_dma_test]
extends = env:papilio_arcade
extra_scripts = pre:src/utilities/examples_select.py
build_src_filter = +<*> -<papliio_arcade_template.ino>
build_flags = 
    ${env:papilio_arcade.build_flags}
    -D PAPILIO_MCP_ENABLED

; (Add future examples by copying an env block and setting src_dir)